apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
spec:
  template:
    spec:
      containers:
        - name: webappcontainer
          imagePullPolicy: Always
      imagePullSecrets:
        - name: ocirsecret
